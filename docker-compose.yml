services:
  web:
    image: ghcr.io/pitchcraftbaseball/pitchcraft-web:${IMAGE_TAG}
    environment:
      DJ_DEBUG: "false"
      DJ_SECRET_KEY: "change-me"
    expose:
      - "8000"

  nginx:
    image: ghcr.io/pitchcraftbaseball/pitchcraft-nginx:${IMAGE_TAG}
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web
